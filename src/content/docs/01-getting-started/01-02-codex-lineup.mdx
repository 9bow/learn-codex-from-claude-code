---
title: Codex 제품 라인업
description: Codex App, IDE 확장, CLI, Cloud의 사용 상황을 한눈에 정리합니다.
sidebar:
  order: 2
learning_objectives:
  - "Codex 주요 사용 형태의 차이와 사용 상황을 구분할 수 있다"
  - "Claude Code와 Codex의 제품 구조 차이를 설명할 수 있다"
prerequisites:
  - "이 가이드 01-01 챕터 읽기"
estimated_minutes: 20
references:
  - title: "Codex Quickstart"
    url: "https://developers.openai.com/codex/quickstart"
    accessed: "2026-05-01"
  - title: "Codex App"
    url: "https://developers.openai.com/codex/app"
    accessed: "2026-05-01"
  - title: "Codex IDE Extension"
    url: "https://developers.openai.com/codex/ide"
    accessed: "2026-05-01"
  - title: "Codex Web"
    url: "https://developers.openai.com/codex/cloud"
    accessed: "2026-05-01"
  - title: "Codex CLI GitHub"
    url: "https://github.com/openai/codex"
    accessed: "2026-05-01"
  - title: "OpenAI Latest Model Guide"
    url: "https://developers.openai.com/api/docs/guides/latest-model.md"
    accessed: "2026-05-16"
---

import ConceptCard from '../../../components/learning/ConceptCard.astro';

## 왜 라인업을 먼저 알아야 하는가

Claude Code는 기본적으로 **하나의 CLI 도구** 입니다. VS Code 확장과 JetBrains 플러그인이 있지만, 핵심은 `claude` 명령어 하나입니다.

Codex는 다릅니다. 공식 Quickstart는 **App, IDE 확장, CLI, Cloud** 네 가지 진입점을 중심으로 안내합니다. ChatGPT 안에서도 Codex 기능을 접할 수 있지만, 전환 학습에서는 이 네 가지를 먼저 구분하는 편이 실용적입니다. 어떤 형태를 선택하느냐에 따라 설정 방법과 기능이 달라지므로, 전환 전에 전체 그림을 파악하는 것이 중요합니다.

## Codex 주요 사용 형태

| 형태 | 실행 방법 | 주 용도 | 공유 설정 |
|------|-----------|---------|-----------|
| **Codex Desktop App** | macOS / Windows 앱 | 멀티 에이전트 관리, diff 검토 | CLI 세션·설정 그대로 활용 |
| **Codex IDE 확장** | VS Code 계열 / JetBrains 패널 | 에디터 내 AI 지원 | `~/.codex/config.toml` 공유 |
| **Codex CLI** | `codex` (터미널) | 로컬 개발, CI/CD, 자동화 스크립트 | `~/.codex/config.toml` |
| **Codex Cloud** | chatgpt.com/codex / CLI | 백그라운드 클라우드 작업, GitHub·Slack 통합 | 별도 클라우드 environment |

:::note[참고]
CLI와 IDE 확장은 **같은 `~/.codex/config.toml`** 을 읽습니다. CLI에서 모델을 바꾸면 IDE 확장에도 즉시 반영됩니다.
:::

## 각 형태 상세

### Codex CLI

Rust로 작성된 오픈소스 CLI입니다. `github.com/openai/codex` 에서 소스를 볼 수 있습니다.

```bash
# 설치
npm install -g @openai/codex

# 기본 실행
codex "테스트를 추가해 줘"

# 비대화형 실행 (CI 등)
codex exec "린트 오류 모두 수정해"
```

Claude Code의 `claude -p "..."` 에 대응하는 비대화형 모드가 `codex exec` 입니다.

### Codex IDE 확장

VS Code, Cursor, Windsurf 같은 VS Code 계열 IDE와 JetBrains IDE 통합을 지원합니다. CLI와 IDE 확장은 같은 Codex 인증 캐시와 설정을 사용합니다.

:::tip[팁]
Profiles 기능(`[profiles.<name>]`)은 현재 **Experimental** 상태이며 IDE 확장에서는 미지원입니다. CLI에서만 `codex --profile <name>` 으로 사용 가능합니다.
:::

### Codex Desktop App

macOS와 Windows에서 사용할 수 있는 GUI 앱입니다. 단순한 에디터 확장과 달리 **멀티 에이전트 명령 센터** 역할을 합니다. ChatGPT 계정 또는 OpenAI API 키로 로그인할 수 있지만, API 키 로그인에서는 cloud threads 같은 일부 기능이 제한될 수 있습니다.

주요 특징:
- 여러 에이전트를 동시에 실행하고 각각의 진행 상황을 관리
- **워크트리 기본 지원**: 각 에이전트가 격리된 코드 복사본에서 작업하므로 로컬 git에 영향 없음
- diff 검토 + 코멘트 + 에디터 즉시 열기
- Skills 생성·관리 전용 UI
- Automations: 스케줄 기반 백그라운드 자동 작업

### Codex Cloud

로컬 환경 없이 클라우드에서 백그라운드로 작업을 실행합니다. GitHub 이슈나 Slack 메시지를 트리거로 Codex 에이전트를 자동 실행하는 통합이 포함됩니다.

### ChatGPT 웹·iOS Codex

ChatGPT 인터페이스에서 코드 작업을 요청하거나 Cloud 작업으로 이어갈 수 있습니다. 다만 이 경로는 Codex CLI/IDE/App의 로컬 설정을 그대로 다루는 전환 학습용 기본 진입점이라기보다, ChatGPT 계정 기반의 보조 진입점으로 이해하는 것이 안전합니다.

:::note[모델 접근성]
OpenAI의 최신 모델과 Codex에서 선택 가능한 모델은 계정, 플랜, API 권한, 클라이언트 버전에 따라 달라질 수 있습니다. 2026년 5월 공식 API 가이드는 `gpt-5.5`를 최신 모델로 안내하므로, "ChatGPT 인증에서만 가능"처럼 고정해 외우기보다 현재 사용 중인 Codex 모델 선택기와 OpenAI 모델 가이드를 함께 확인하세요.
:::

<ConceptCard title="Claude Code와 제품 구조 비교">

**Claude Code**:
- `claude` CLI 하나가 핵심
- VS Code 확장 (`anthropic.claude-code`), JetBrains 플러그인은 CLI 래퍼
- 설정: `~/.claude/settings.json`

**Codex**:
- App / IDE 확장 / CLI / Cloud를 중심으로 사용
- CLI + IDE 확장은 `~/.codex/config.toml` 공유
- Desktop App은 CLI 세션·설정을 그대로 활용
- Cloud와 ChatGPT는 별도 인증 체계

</ConceptCard>

## 어떤 형태를 선택해야 하는가

| 상황 | 권장 형태 |
|------|-----------|
| 기존 Claude Code처럼 터미널 중심 개발 | **Codex CLI** |
| 에디터를 벗어나지 않고 AI 지원 | **IDE 확장** |
| 여러 에이전트를 동시에 관리하고 싶다 | **Desktop App** |
| GitHub Actions, CI 파이프라인 통합 | **Codex Cloud** |
| 가볍게 코드 관련 질문·수정 | **ChatGPT 웹·앱 또는 Codex App** |

Claude Code에서 전환하는 경우 대부분은 **Codex CLI** 부터 시작하는 것이 가장 빠른 적응 경로입니다.

## 핵심 정리

- Codex 전환 학습에서는 App, IDE 확장, CLI, Cloud **네 가지 진입점** 을 먼저 구분합니다
- **CLI와 IDE 확장** 은 `~/.codex/config.toml` 을 공유합니다
- **Desktop App** 은 멀티 에이전트 관리와 워크트리 격리를 기본 지원합니다
- **Codex Cloud** 는 GitHub·Slack 통합 백그라운드 작업에 적합합니다
- Claude Code 전환자에게는 **Codex CLI** 가 가장 친숙한 출발점입니다
- 모델 접근성은 계정·플랜·API 권한에 따라 달라지므로 현재 모델 가이드와 클라이언트 모델 선택기를 함께 확인해야 합니다
